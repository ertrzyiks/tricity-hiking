---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
import ListCardItem from "../ListCardItem.astro";

const { link, icon, image, name, description } = Astro.props;
---

<ListCardItem link={link}>
  {
    image ? (
      <Image
        src={image}
        width="160"
        height="160"
        class="w-20 h-20 grayscale-50 group-hover:grayscale-0"
        alt={`Photo of ${name}`}
        slot="image"
      />
    ) : (
      <div
        slot="image"
        class="bg-green-200 text-slate-900 w-16 h-16 flex items-center justify-center group-hover:bg-black group-hover:text-green-200"
      >
        <Icon name={icon} class="w-12 h-12" />
      </div>
    )
  }

  <span class="group-hover:text-accent-strong" slot="title">
    {name}
  </span>

  <span class="group-hover:text-accent-strong" slot="description">
    {description}
  </span>
</ListCardItem>
