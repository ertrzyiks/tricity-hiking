---
const { href, active } = Astro.props;
---

<a
  class:list={[
    "px-6 sm:px-12 py-4 text-sm font-medium inline-flex items-center gap-2 relative border-t-2 border-l border-r border-l-gray-300 border-r-gray-300 whitespace-nowrap snap-start",
    {
      "text-green-600 border-t-green-600": active,
      "text-gray-900 bg-gray-200 border-t-transparent": !active,
    },
  ]}
  href={href}
  data-active={active ? "true" : undefined}
>
  <slot />
</a>
