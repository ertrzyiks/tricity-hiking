---
const { href, active } = Astro.props;
---

<a
  class:list={[
    "px-6 sm:px-12 py-4 text-sm font-medium inline-flex items-center gap-2 relative -mb-0.5 border-t-2 border-l border-r border-l-gray-200 border-r-gray-200 whitespace-nowrap snap-start",
    {
      "text-green-600 border-t-green-600": active,
      "text-gray-900 bg-gray-200 border-t-transparent": !active,
    },
  ]}
  href={href}
>
  <slot />
</a>
