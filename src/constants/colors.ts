/**
 * Color constants used across the application
 */

// Map marker colors
export const MAP_MARKER_COLOR = "#b91c1c"; // red-700 - darker red for map icons (start, end, loop markers)

// Default color of the route line itself, before the visitor picks a
// different one with the RouteMap color picker.
export const ROUTE_LINE_COLOR = "#e11d48"; // rose-600
