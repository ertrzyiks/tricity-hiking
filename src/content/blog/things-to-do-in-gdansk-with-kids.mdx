---
title: "Things to do in Gdańsk with kids"
description: "Tricity has a lot of playgrounds for kids. The list below highlights the biggest or the most interesting ones."
publishDate: 2026-08-20
draft: false
---

import ActivityList from "../../components/ui/ActivitiesList/ActivityList.astro";
import { getCollection } from "astro:content";

export const activities = await getCollection(
  "activities",
  ({ data }) =>
    !data.draft &&
    (data.category === "outdoor-playground" ||
      data.category === "indoor-playground"),
);

<ActivityList activities={activities} groupBy="category" />
